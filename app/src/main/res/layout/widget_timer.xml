<?xml version="1.0" encoding="utf-8"?>
<!-- 1×1: кольцо обратного отсчёта. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/wt_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:padding="3dp">

    <ProgressBar
        android:id="@+id/wt_ring_lesson"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:progressDrawable="@drawable/ring_lesson"
        android:max="100"
        android:progress="65" />

    <ProgressBar
        android:id="@+id/wt_ring_break"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:progressDrawable="@drawable/ring_break"
        android:max="100"
        android:progress="0"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/wt_main"
            android:text="42"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:textColor="@color/w_text" />

        <TextView
            android:id="@+id/wt_sub"
            android:text="мин"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="9sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/w_sub" />
    </LinearLayout>
</FrameLayout>
